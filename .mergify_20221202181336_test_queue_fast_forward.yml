pull_request_rules:
- actions:
    queue:
      method: fast-forward
      name: default
      priority: high
  conditions:
  - base=20221202181336/test_queue_fast_forward/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
