jobs:
  test-dispatch:
    runs-on: ubuntu-latest
    steps:
    - name: Test dispatched workflow inputs
      run: echo I really like my CI workflow
name: continuous_integration
'on':
  pull_request:
    branches: 20231221170852/test_gha_success/main
  workflow_dispatch:
    inputs:
      some_number:
        required: true
        type: number
      some_string:
        required: true
        type: string
