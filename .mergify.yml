pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20221125094852/test_queue_with_queue_branch_prefix/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_branch_prefix: mq-
  speculative_checks: 5
