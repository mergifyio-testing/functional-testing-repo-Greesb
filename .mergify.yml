pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20221220081209/test_start_at_end_at_query_params/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
