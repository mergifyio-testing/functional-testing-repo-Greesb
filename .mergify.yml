merge_queue:
  max_parallel_checks: 5
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20241104135825/test_time_to_merge_endpoint_normal/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  batch_size: 2
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
