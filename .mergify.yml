pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - check-success=continuous-integration/fake-ci
  - base=20240315162228/test_queue_auto_reembark/main
  name: Queue
queue_rules:
- conditions:
  - check-success=second-ci
  name: default
  speculative_checks: 1
