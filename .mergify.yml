pull_request_rules:
- actions:
    queue:
      merge_method: fast-forward
      name: default
  conditions:
  - base=20231121130143/TQAWPEItest_queue_fast_forward_with_retries/main
  - label=queue
  name: merge fast-forward
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
