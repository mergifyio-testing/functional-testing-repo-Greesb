pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231121124152/TQAWPEItest_queue_cancel_and_refresh_inplace/main
  - status-success=continuous-integration/fake-ci
  name: Tchou tchou
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
