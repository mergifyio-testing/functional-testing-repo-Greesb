pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315172741/test_queue_command_after_action_queue/main
  - label=queue
  name: Merge on queue label
queue_rules:
- conditions: []
  name: default
