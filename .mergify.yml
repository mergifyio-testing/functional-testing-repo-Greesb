pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318102630/test_ongoing_train_second_pr_ready_first/main
  - label=queue
  name: merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
