pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240118163630/test_queue_fast_forward_with_required_pull_request_branch_protection/main
  - label=queue
  name: merge fast-forward
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_branch_merge_method: fast-forward
