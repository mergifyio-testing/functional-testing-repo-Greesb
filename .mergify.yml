pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230530124341/test_freeze_eventlogs/main
  - label=queue
  name: queueit
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
