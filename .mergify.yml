pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315182713/test_unqueue_then_requeue_not_in_first_place_check_run/main
  - label=queue
  name: Queue on label
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
