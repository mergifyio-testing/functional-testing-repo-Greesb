pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20221115092707/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
