pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - body-raw~=.*good to merge.* 
    actions:
      comment:
        message: Good to merge
      merge:
        method: merge