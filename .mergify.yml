merge_queue:
  queued_label: null
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251016124749/test_conflicts_with_pull_ahead/main
  - label=queue
  name: Queue on label
- actions:
    label:
      add:
      - queue-conflict
  conditions:
  - queue-dequeue-reason=conflict-with-pull-ahead
  name: Label queue conflicts
queue_rules:
- batch_size: 2
  conditions: []
  name: default
