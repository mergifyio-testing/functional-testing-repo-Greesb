pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231121140347/TQAWPEItest_train_reset_after_unexpected_base_branch_pushed_while_merging_batch/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
