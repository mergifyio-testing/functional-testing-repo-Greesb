pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251020152730/test_base_branch_vanished/featureA
  name: merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
