pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230328082555/test_update_pr_in_queue/main
  - label=queue
  name: Automatic merge
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
