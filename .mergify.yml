pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231121114524/TQAWPEItest_base_branch_vanished/featureA
  name: merge default
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
