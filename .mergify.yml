merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251028093450/test_unqueue_on_synchronise_and_rule_unmatch/main
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=other-ci
  name: default
