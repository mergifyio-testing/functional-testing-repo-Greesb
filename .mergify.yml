pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251016123807/test_batch_cant_create_tmp_pull_request/main
  - label=queue
  name: merge default
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
