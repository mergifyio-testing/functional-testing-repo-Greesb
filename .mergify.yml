pull_request_rules:
- actions:
    queue:
      name: urgent
  conditions:
  - base=20221007090348/test_queue_priority/main
  - label=queue-urgent
  name: Merge priority high
- actions:
    queue:
      name: default
  conditions:
  - base=20221007090348/test_queue_priority/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fast-ci
  name: urgent
  speculative_checks: 5
- conditions:
  - status-success=continuous-integration/slow-ci
  name: default
  speculative_checks: 5
