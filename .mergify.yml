pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240118084546/test_cerebras_bug/main
  - check-success=continuous-integration/fake-ci
  name: Merge default
queue_rules:
- allow_inplace_checks: false
  batch_size: 4
  merge_conditions:
  - check-success=continuous-integration/fake-ci
  name: default
