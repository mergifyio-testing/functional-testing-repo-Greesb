pull_request_rules:
- actions:
    queue: {}
  conditions:
  - status-success=continuous-integration/fake-ci
  name: Queue
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
