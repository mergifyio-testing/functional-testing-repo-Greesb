pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315171455/test_merge_queue_refresh/main
  - label=queue
  name: default merge
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
