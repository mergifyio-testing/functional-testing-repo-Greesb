pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
      update_bot_account: "{%\_if 1 -%}mergify-test4{% endif -%}"
      update_method: rebase
  conditions:
  - base=20230130081542/test_queue_with_rebase_update_method/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
