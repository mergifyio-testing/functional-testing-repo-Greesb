defaults:
  actions:
    comment: {}
pull_request_rules:
- actions:
    comment:
      message: Hello World!
  conditions:
  - base=20231201165735/test_comment_without_default_message/main
  name: comment without default message
