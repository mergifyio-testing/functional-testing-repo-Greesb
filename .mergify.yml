pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20220812133856/test_checks_duration_endpoint/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  name: default
  speculative_checks: 1
