merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
      update_method: rebase
  conditions:
  - base=20250606114403/test_queue_update_inplace_merge_report/main
  - status-success=continuous-integration/fake-ci
  name: default merge
queue_rules:
- conditions:
  - status-success=another-ci
  name: default
