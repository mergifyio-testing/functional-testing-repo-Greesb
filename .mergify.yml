pull_request_rules:
- actions:
    merge:
      method: rebase
  conditions:
  - or:
    - label=foo
    - and:
      - or:
        - base:foo
        - and:
          - base:baz
          - base=main
          - or:
            - author=bot
            - and:
              - author:robot
              - label=robot
              - or:
                - '#approved-reviews-by>=1'
                - or:
                  - '#review-threads-unresolved=0'
                  - label=skip
      - label=bar
  name: huge nest
