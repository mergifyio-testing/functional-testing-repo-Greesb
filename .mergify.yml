pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240905182529/test_dequeue_no_partitions/main
  name: Queue
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
