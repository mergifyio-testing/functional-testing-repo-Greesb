pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231207093726/test_freeze_eventlogs/main
  - label=queue
  name: queueit
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
