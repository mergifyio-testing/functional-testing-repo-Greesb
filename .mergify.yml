pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315181930/test_unqueue_rule_unmatches/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
