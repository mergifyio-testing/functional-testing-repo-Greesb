pull_request_rules:
- actions:
    queue: {}
  conditions:
  - label=queue
  name: Queue
queue_rules:
- name: pending-check-success
  routing_conditions:
  - check-success=some-pending-check-in-ci
- name: default
  routing_conditions:
  - label=will-not-be-set
