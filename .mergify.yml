merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - check-success=continuous-integration/fake-ci
  - base=20250730082100/test_queue_auto_reembark/main
  name: Queue
queue_rules:
- conditions:
  - check-success=second-ci
  name: default
