pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230404151959/test_accuracy_measure/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
