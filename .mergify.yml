merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251016142205/test_queue_cancel_and_refresh_inplace/main
  - status-success=continuous-integration/fake-ci
  name: Tchou tchou
queue_rules:
- name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
