merge_queue:
  max_parallel_checks: 1
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251016124843/test_queue_auto_reembark/main
  name: Queue
queue_rules:
- name: default
  queue_conditions:
  - check-success=continuous-integration/fake-ci
