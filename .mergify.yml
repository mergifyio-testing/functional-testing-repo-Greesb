merge_queue:
  max_parallel_checks: 5
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20241029162516/test_queue_cancel_and_refresh/main
  - label=queue
  name: Tchou tchou
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci
