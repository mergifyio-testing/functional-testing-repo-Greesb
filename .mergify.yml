pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20231228161325/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
