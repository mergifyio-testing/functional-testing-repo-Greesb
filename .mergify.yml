partition_rules:
- conditions:
  - files~=^projA/
  name: projA
- conditions:
  - files~=^projB/
  name: projB
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240317205008/test_time_to_merge_with_partitions_endpoint_normal/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  batch_size: 2
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
