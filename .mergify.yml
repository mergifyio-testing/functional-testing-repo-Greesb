pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315171629/test_queue_cancel_and_refresh_inplace/main
  - status-success=continuous-integration/fake-ci
  name: Tchou tchou
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
