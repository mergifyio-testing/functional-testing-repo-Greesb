pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240708152634/test_basic_queue/main
  - label=queue
  name: default merge
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
