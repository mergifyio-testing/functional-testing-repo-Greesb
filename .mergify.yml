_checks_to_retry_on_failure:
  unit-tests: 2
pull_request_rules:
- actions:
    queue:
      name: foo
  conditions:
  - base=20231127205828/test_rerun_on_known_flaky/main
  name: queue
queue_rules:
- merge_conditions:
  - check-success=unit-tests
  name: foo
