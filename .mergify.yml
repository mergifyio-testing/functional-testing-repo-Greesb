pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20241106080807/test_required_status_checks_strict_incompatibility_with_batch_size/main
  name: Merge
queue_rules:
- allow_inplace_checks: true
  batch_size: 2
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
