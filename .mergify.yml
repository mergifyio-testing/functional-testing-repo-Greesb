pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220803140424/test_statistics_present/main
  - check-success=continuous-integration/fake-ci
  name: queue
queue_rules:
- checks_timeout: 10 m
  conditions:
  - check-success=continuous-integration/fake-ci
  name: default
