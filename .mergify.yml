partition_rules:
- conditions:
  - files~=projA/
  name: projA
- conditions:
  - files~=projB/
  name: projB
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230309134405/test_two_prs_in_differents_partitions_merged/main
  - label=queue
  name: Automatic merge
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
