pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318104149/test_queue_ci_failure/main
  - label=queue
  name: merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
