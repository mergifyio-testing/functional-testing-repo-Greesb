pull_request_rules:
- actions:
    queue:
      merge_bot_account: anakin
      name: default
  conditions:
  - base=20231228161749/test_report_error_bot_account_wrong_permissions/main
  - label=queue
  name: Merge default
queue_rules:
- merge_conditions:
  - status-success=continuous-integration/fake-ci-merge
  name: default
  queue_conditions:
  - status-success=continuous-integration/fake-ci-queue
