pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318115249/test_queue_cant_create_tmp_pull_request/main
  - label=queue
  name: merge default
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
