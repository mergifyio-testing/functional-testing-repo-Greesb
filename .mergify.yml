pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318110153/test_queue_with_allow_queue_branch_edit_set_to_true/main
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  allow_queue_branch_edit: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
