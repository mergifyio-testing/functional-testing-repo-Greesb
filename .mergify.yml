pull_request_rules:
- actions:
    label:
      toggle:
      - CI:fail
  conditions:
  - base=20231227154114/test_label_toggle/main
  - check-failure=continuous-integration/fake-ci
  name: toggle labels
