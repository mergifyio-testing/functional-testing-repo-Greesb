_checks_to_retry_on_failure:
  unit-tests: 2
pull_request_rules:
- actions:
    queue:
      name: foo
  conditions:
  - base=20231228132821/test_rerun_on_known_flaky_with_api/main
  name: queue
queue_rules:
- merge_conditions:
  - check-success=unit-tests
  name: foo
