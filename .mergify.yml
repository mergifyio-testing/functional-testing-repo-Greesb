pull_request_rules:
- actions:
    queue:
      name: default
      update_method: rebase
  conditions:
  - base=20240118165040/test_queue_fast_forward_with_required_pull_request_branch_protection/main
  - label=queue
  name: merge fast-forward
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  queue_branch_merge_method: fast-forward
