pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20221004092152/test_queue_with_ci_in_pull_request_rules/main
  - label=queue
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
