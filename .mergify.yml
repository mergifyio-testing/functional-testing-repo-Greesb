pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240708150602/test_batch_split_queue/main
  - label=queue
  name: default merge
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 2
