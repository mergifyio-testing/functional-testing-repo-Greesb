pull_request_rules:
- actions:
    github_actions:
      workflow:
        dispatch:
        - inputs:
            author: '{{author}}'
          ref: 20240129151840/test_gha_with_template_input/main
          workflow: dynamic_workflow.yaml
  conditions:
  - label=dispatch
  name: Dispatch GHA
