pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20231129084519/TQAWPEItest_unqueue_command_with_batch_requeue/main
  - label=queue
  name: default merge
queue_rules:
- allow_inplace_checks: true
  batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
