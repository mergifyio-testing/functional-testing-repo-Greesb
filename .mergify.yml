pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20220923171531/test_queue_ci_failure/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
