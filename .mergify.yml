pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220805133118/test_statistics_format_in_redis/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  conditions:
  - check-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
