pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20220818113647/test_checks_duration_endpoint/main
  - check-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  conditions:
  - check-success=continuous-integration/fake-ci
  name: default
