partition_rules:
- conditions:
  - files~=projA/
  name: projA
- conditions:
  - files~=projB/
  name: projB
pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240317212344/test_dequeue_with_partition_rules/main
  - label=queue
  name: Automatic merge
queue_rules:
- allow_inplace_checks: false
  merge_conditions:
  - status-success=continuous-integration/fake-ci
  name: default
