pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230424082237/test_base_branch_conflicts_check_run_message_draft/main
  - label=queue
  name: Merge on queue label
queue_rules:
- allow_inplace_checks: true
  conditions: []
  name: default
