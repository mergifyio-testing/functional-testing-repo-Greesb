pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20251020162032/test_unqueue_rule_unmatch_with_batch_requeue/main
  - label=queue
  name: default merge
queue_rules:
- batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
