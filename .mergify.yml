pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230523120536/test_start_at_end_at_query_params/main
  - label=queue
  name: merge default
queue_rules:
- allow_inplace_checks: true
  batch_max_wait_time: 0 s
  batch_size: 3
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 1
