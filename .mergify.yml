pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318121059/test_train_reset_after_unexpected_base_branch_pushed_while_merging_batch/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_size: 2
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
