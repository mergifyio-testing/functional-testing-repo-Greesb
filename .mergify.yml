pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240315174816/test_queue_manual_merge/main
  - label=queue
  name: merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
