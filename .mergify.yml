pull_request_rules:
- actions:
    comment:
      message: no non-verified commits allowed
  conditions:
  - -commits[*].verified
  name: no non verified
- actions:
    comment:
      message: verified is life
  conditions:
  - commits[*].verified
  name: yes verified
