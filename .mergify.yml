pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20221125095449/test_queue_with_bot_account/main
  - label=queue
  name: Merge priority high
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  draft_bot_account: mergify-test4
  name: default
  speculative_checks: 5
