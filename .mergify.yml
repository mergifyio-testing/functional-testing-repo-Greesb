pull_request_rules:
- actions:
    comment:
      message: commits unverified
  conditions:
  - 'commits-unverified="test_one_commit_unverified_message: pull request n1 from
    integration"'
  name: commits-unverified
