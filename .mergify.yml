pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230523093953/test_queue_rule_deleted/main
  name: Merge me
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
  speculative_checks: 5
