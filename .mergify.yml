pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230330132619/test_queue_command_after_action_queue/main
  - label=queue
  name: Merge on queue label
queue_rules:
- allow_inplace_checks: false
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
