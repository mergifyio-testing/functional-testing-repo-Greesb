pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20230530084206/test_pr_embarked_check_runs_statuses_ci_summary/main
  - label=queue
  name: merge default
queue_rules:
- conditions:
  - status-success=continuous-integration/fail-ci
  - status-success=continuous-integration/pending-ci
  - status-success=continuous-integration/success-ci
  name: default
  speculative_checks: 5
