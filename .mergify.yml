pull_request_rules:
- actions:
    queue: {}
  conditions:
  - label=queue
  name: automatic merge
queue_rules:
- allow_inplace_checks: false
  merge_conditions:
  - check-success=continuous-integration/fake-ci
  name: default
