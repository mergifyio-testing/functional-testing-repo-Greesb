pull_request_rules:
- actions:
    queue:
      name: default
  conditions:
  - base=20240318121632/test_unqueue_on_synchronise_and_rule_unmatch/main
  - status-success=continuous-integration/fake-ci
  name: Merge priority high
queue_rules:
- allow_inplace_checks: true
  conditions:
  - status-success=other-ci
  name: default
